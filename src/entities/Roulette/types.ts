export type SegmentType = {
  label: string;
  color: string;
};

export type RouletteOptions = {
  TOTAL_SIZE: number;
  BORDER_WIDTH: number;
  CENTER: number;
  WHEEL_SIZE: number;
  EXTRA_PADDING: number;
  RADIUS: number;
  TEXT_RADIUS: number;
};
